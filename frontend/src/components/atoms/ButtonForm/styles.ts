import styled from 'styled-components';

export const StyledButton = styled.button`
    background: linear-gradient(to right, #5656FF 0%, #03217b 79%);
    text-transform: uppercase;
    letter-spacing: 0.2rem;
    width: 80%;
    height: 3rem;
    border: none;
    color: white;
    border-radius: 2rem;
    cursor: pointer;
`;
